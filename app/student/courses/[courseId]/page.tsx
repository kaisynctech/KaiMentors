import{BookOpen,CheckCircle2,Clock3,PlayCircle}from"lucide-react";import Image from"next/image";import Link from"next/link";import{notFound,redirect}from"next/navigation";import{BrandMark}from"@/components/brand-mark";import{formatDuration}from"@/lib/courses";import{createAdminClient}from"@/lib/supabase/admin";import{createClient}from"@/lib/supabase/server";import{getStudentAcademyContext}from"@/lib/student-routing";import styles from"../courses.module.css";
export default async function StudentCoursePage({params,searchParams}:{params:Promise<{courseId:string}>;searchParams?:Promise<{portal?:string}>}){const{courseId}=await params;const query=await searchParams;const academy=await getStudentAcademyContext(query?.portal);const supabase=await createClient();if(!supabase)redirect("/login");const{data:{user}}=await supabase.auth.getUser();if(!user)redirect("/login");let aq=supabase.from("student_applications").select("trader_id,portal:portals!inner(portal_name,slug)").eq("student_user_id",user.id).eq("status","verified");if(academy.portalId)aq=aq.eq("portal_id",academy.portalId);if(academy.portalSlug)aq=aq.eq("portal.slug",academy.portalSlug);const{data:app}=await aq.limit(1).maybeSingle();if(!app)redirect(`${academy.basePath}${academy.querySuffix}`);
 const[{data:course},{data:modules},{data:lessons},{data:progress}]=await Promise.all([supabase.from("courses").select("id,title,description,cover_path").eq("id",courseId).eq("trader_id",app.trader_id).eq("status","published").maybeSingle(),supabase.from("course_modules").select("id,title,description,sort_order,is_required").eq("course_id",courseId).eq("trader_id",app.trader_id).eq("status","published").order("sort_order"),supabase.from("lessons").select("id,module_id,title,description,duration_seconds,sort_order,is_required").eq("course_id",courseId).eq("trader_id",app.trader_id).eq("status","published").order("sort_order"),supabase.from("lesson_progress").select("lesson_id,is_completed,position_seconds").eq("course_id",courseId).eq("student_user_id",user.id)]);if(!course)notFound();let thumbnailUrl:null|string=null;const admin=createAdminClient();if(course.cover_path&&admin){const{data:signed}=await admin.storage.from("course-content").createSignedUrl(course.cover_path,300);thumbnailUrl=signed?.signedUrl??null}const required=(lessons??[]).filter(l=>l.is_required);const completed=required.filter(l=>(progress??[]).some(p=>p.lesson_id===l.id&&p.is_completed)).length;const percent=required.length?Math.round(completed/required.length*100):0;const portal=Array.isArray(app.portal)?app.portal[0]:app.portal;const base=academy.basePath;const suffix=academy.querySuffix;
 return <main className={styles.page}><nav className={styles.nav}><BrandMark href={`${base}/courses${suffix}`} label={base==="/academy"?portal?.portal_name??"Academy":"KaiMentors"}/><div className={styles.navActions}><Link href={`${base}/courses${suffix}`}>My Learning</Link><Link href={`${base}/messages${suffix}`}>Messages</Link><Link href="/auth/signout">Sign out</Link></div></nav><section className={`${styles.content} ${styles.detail}`}><div className={styles.detailCover}>{thumbnailUrl?<Image alt="" fill sizes="(max-width:900px) 100vw,440px" src={thumbnailUrl} unoptimized/>:<BookOpen/>}</div><div className={styles.detailCopy}><p className="eyebrow">Protected course</p><h1>{course.title}</h1><p>{course.description||"Work through the curriculum below."}</p><div className={styles.percent}><span style={{width:`${percent}%`}}/></div><strong>{percent}% complete · {completed}/{required.length} required lessons</strong></div></section><section className={`${styles.content} ${styles.curriculum}`}>{(modules??[]).map(module=><div className={styles.moduleStudent} key={module.id}><header><div><p className="eyebrow">Module {module.sort_order}</p><h2>{module.title}</h2></div><span>{module.is_required?"Required":"Optional"}</span></header>{(lessons??[]).filter(l=>l.module_id===module.id).map(lesson=>{const done=(progress??[]).some(p=>p.lesson_id===lesson.id&&p.is_completed);return <Link className={styles.lesson} href={`${base}/courses/${course.id}/lessons/${lesson.id}${suffix}`} key={lesson.id}><div className={styles.lessonNumber}>{done?<CheckCircle2/>:lesson.sort_order}</div><div><h3>{lesson.title}</h3><p>{lesson.description||"Mixed-media lesson"}</p></div><span><Clock3/> {formatDuration(lesson.duration_seconds)} <PlayCircle/></span></Link>})}</div>)}{!modules?.length?<div className={styles.empty}><PlayCircle/><h2>No published curriculum</h2><p>Your mentor is still preparing this course.</p></div>:null}</section></main>}
